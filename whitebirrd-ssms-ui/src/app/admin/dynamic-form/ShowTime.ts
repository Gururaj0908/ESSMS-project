export enum ShowTime {
   // will be shown only when the data doesn't exist
   NewDataOnly,
   // will be shown only when the data is present
   EditDataOnly,
   // will be always shown
   BothPlaces
}
